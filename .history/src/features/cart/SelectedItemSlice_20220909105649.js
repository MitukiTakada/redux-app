import { createSlice } from "@reduxjs/toolkit";

const initialstate = {
  selectedItem: 1
}

const SelectedItemSlice = createSlice({
  name: "selectedItem",
  initialstate,
  reducers : {
    selectItem : (state) => {
      se
    }
  }
})