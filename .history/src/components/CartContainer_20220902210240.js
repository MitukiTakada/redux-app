import React from 'react'

const CartContainer = () => {
  if(amount < 1) {
    return (
      <section>
        <header>
          <
        </header>
      </section>
    )
  }
};

export default CartContainer
