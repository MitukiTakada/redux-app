import React from 'react'

const CartContainer = () => {
  if(amount < 1) {
    return (
      <section>
        <header>
          <h</h2>
        </header>
      </section>
    )
  }
};

export default CartContainer
